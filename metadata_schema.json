### metadata_schema.json

```json
{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Rogue NFT Metadata Schema",
  "type": "object",
  "required": [
    "name",
    "description",
    "image",
    "attributes",
    "metadata_version",
    "prompt_version"
  ],
  "properties": {
    "name": {
      "type": "string",
      "description": "Human readable name of the NFT"
    },
    "description": {
      "type": "string",
      "description": "Short description used by marketplaces"
    },
    "image": {
      "type": "string",
      "format": "uri",
      "description": "IPFS or HTTP pointer to the image asset (prefer ipfs://)"
    },
    "animation_url": {
      "type": "string",
      "format": "uri",
      "description": "Optional pointer to animated asset or video"
    },
    "attributes": {
      "type": "array",
      "description": "Array of trait objects",
      "items": {
        "type": "object",
        "required": ["trait_type", "value"],
        "properties": {
          "trait_type": { "type": "string" },
          "value": { "type": ["string", "number", "boolean"] }
        }
      }
    },
    "unlockable_content_ipfs": {
      "type": "string",
      "format": "uri",
      "description": "IPFS pointer to gated lore or unlockable file"
    },
    "external_url": {
      "type": "string",
      "format": "uri",
      "description": "Optional external page for the NFT"
    },
    "tags": {
      "type": "array",
      "items": { "type": "string" }
    },
    "metadata_version": {
      "type": "string",
      "description": "Schema version, e.g., 1.0"
    },
    "prompt_version": {
      "type": "string",
      "description": "Version tag for the prompt used to generate this asset"
    },
    "minting": {
      "type": "object",
      "properties": {
        "chain": { "type": "string", "enum": ["polygon", "ethereum", "solana"] },
        "contract_address": { "type": "string" },
        "token_id": { "type": ["string", "number"] },
        "transaction_hash": { "type": "string" }
      }
    },
    "created_at": {
      "type": "string",
      "format": "date-time"
    }
  },
  "additionalProperties": false
}
```

---
